v0.7.3: Error handling for `bmd` and dependency update

🐛 Bug fixes
- Fix `bmd` error handling to exit with non-zero code when commands fail
- Print stderr to stderr when commands fail (when `include_stderr=False`)
- Aligns `bmd` error handling with `mdcmd` improvements from v0.7.2

📦 Dependencies
- Bump `utz` requirement to `>=0.21.4`
